# DSH 开发路线图 v1.0（全员 DSH 版）

> 定位：把《DESP技能规范接口v1.0.md》《all_skill总集成文档.md》《团队分工与交付说明.md》落到 DSH 上的**行动手册**。
> 阅读对象：5 名成员 + 指导老师。
> 前提：每人电脑 Node.js 可用、模型 API key 就绪、有 git、有课程/导师指导 DSH 基础操作。

---

## 1. 开发范式（先统一认知，别用错力气）

**DSH 里干活的是 AI，我们 5 个人的角色是"产品经理 + 验收员 + 指挥"：**

```
每个成员 = 给 AI 下指令的人
AI（DSH 会话）= 实现者：读你的 ROLE 和契约 → 干活 → 产出文件 → 按你的要求修改
人 = 验收：检查产出是否符合 all_skill 的数据契约；不符合就把问题丢回给 AI
```

**这意味着每个人要练的不是写代码，而是三件事：**
1. 下清晰指令（把 ROLE / 输入 / 期望输出说清楚，契约文件就是现成的"话术"）；
2. 验收（对照 `all_skill` 附录 A 逐字段核对）；
3. 排错（把报错原样丢回给 AI，让它修）。

---

## 2. 仓库结构（合并的物理基础）

```
hr-agent-team/
├── docs/                          # 三份契约文档（已冻结）
│   ├── DESP技能规范接口v1.0.md
│   ├── all_skill总集成文档.md
│   └── 团队分工与交付说明.md
├── agents/
│   ├── analyst/                   # 成员 A：招聘分析师
│   ├── scout/                     # 成员 B：简历猎手
│   ├── interviewer/               # 成员 C：面试官
│   ├── assessor/                  # 成员 D：测评背调员
│   └── concierge/                 # 成员 E：offer 与入职管家
├── shared/
│   ├── registry/                  # 技能注册表 skills.yaml
│   ├── state/                     # 任务总线：候选人档案 JSON、任务状态
│   ├── knowledge/                 # 团队背景、市场薪资、术语白皮书
│   └── style/                     # 风格规则（供 ROLE 引用）
├── dispatcher/                    # 调度器：编排脚本 + 说明
└── demo/                          # 演示视频、截图、设计文档、成果工作台
```

---

## 3. 每个成员的模块长什么样（自己文件夹内产出物）

> 每个文件都**让 AI 按文档帮你写**，你负责验收。模板如下：

### `agents/<你>/ROLE.md`（角色提示词，最重要）

固定格式，写清 6 块：

```markdown
# 角色：{数字员工名}
1. 我是谁：{岗位职责一句话}
2. 我负责的技能：{技能 id 列表}
3. 我的输入：{引用 all_skill 2.2 数据契约，贴字段}
4. 我的输出：{贴 output 契约 + 枚举，注明必须合法}
5. 我的风格：{结论→依据→风险→下一步；术语表遵守 shared/style}
6. 禁止事项：{不得编造证据；不确定就输出 hold + reason}
```

### `agents/<你>/skills/<skill>.md`（每个技能一张卡）

```markdown
# 技能：resume.score
输入样例：{JSON，对齐契约}
期望输出：{JSON，含合法枚举与 reason}
判定规则：{多少分以上 shortlist / 以下 reject 等，你定，别人按这个验收}
```

### `agents/<你>/samples/`（自测数据，至少 3 组）

输入 JSON + 期望输出 JSON，合并阶段拿来当"集成测试用例"。

### `agents/<你>/demo/`（示例成果，让 AI 帮你生成）

先手工造 1 组完整数据 → 让 AI 按你的技能产出真实成果文件：
`demo/需求澄清报告.md`、`demo/初筛打分表.csv`…（类型见分工文档第 7 节）。

---

## 4. 单人自测（每个成员 D3–6 的出口标准）

在你自己的 DSH 会话里做这件事：

```
把 ROLE.md 全文 + 一份 samples/ 输入 发给 AI
→ AI 应产出符合契约的输出
→ 你对照 all_skill 附录 A 逐字段验收
→ 不合格就要求 AI 按契约重做，直到通过
```

**出口标准**：你的 2~3 个技能，各自用 3 组输入跑通、产出成果文件、通过字段核对。**这时你的模块才算"可交付"**，可以进入合并。

---

## 5. 共享层：任务总线与共享状态

- `shared/state/candidates/<candidate_id>.json` = 一份候选人档案，按 all_skill 2.3 的 `CandidateRecord` 结构；
- 状态流转：谁完成了就更新 `status`（枚举见 all_skill 附录 B），写回该 JSON；
- **解耦铁律**：5 个 agent 不直接互相传数据，都只读写这个档案文件 + 各自的输入输出 JSON。

---

## 6. 调度器（D8–9 合并阶段实现）

思路：一个"编排脚本/流程"，按依赖链把任务逐个派给 5 个数字员工。逻辑骨架（具体实现语法以课程教的 DSH 编排方式为准）：

```text
1. 读取用户模糊指令
2. 按状态机决定下一步（初始：demand.clarify）
3. 取当前技能的 input_schema，从 state 组好 payload
4. 把 ROLE + payload 发给对应数字员工（A→B→C→D→E）
5. 拿回结果 → 按 output_schema 校验 → 不合格退回重做（最多 1 次）
6. 合格 → 写回 shared/state，更新状态键 → 进入下一步
7. 循环直到 deliverable 齐 → 触发质检关卡（术语/枚举/四段式）→ 生成成果工作台 HTML
```

**人工确认节点（human-in-the-loop）**：在 `offer.generate` 之后暂停，等人在界面上确认"同意发送 offer"再继续——这是赛道维度要的"不是无人值守，是数字员工"。

---

## 7. 成果工作台（演示门面）

合并完成后，让 AI 生成一个 `demo/workbench.html`：
- 顶部：本次招聘的一句话任务 + 状态时间线（A→E 谁在什么时候干了什么）；
- 中部：13 类成果的下载/打开链接（md / csv / html）；
- 底部：可验收性清单打勾（每份成果的验收方式）。

评委打开这个页面 = 看到"报告、表格、网页"全齐 + 全程可追溯。这就是可验收性的直观证据。

---

## 8. 两周执行表

| 天 | 全员动作 | 出口标准 |
|---|---|---|
| D1 | 每人装环境；`dsh web` 打开界面；让 AI 生成一个文件（Hello） | 每台电脑能跑通 |
| D2 | 建 git 仓库、克隆；**契约冻结会**：逐字段过 all_skill 2.2 + 附录，签字 | 契约无争议，文档进 docs/ |
| D3–4 | 让 AI 写各自 ROLE.md + skills/*.md + samples/ | 模块定义文件齐 |
| D5–6 | 单人自测：3 组输入跑通 + demo/ 成果文件产出 | 单人闭环达标（第 4 节出口标准）|
| D7 | 两两对接：把上游 JSON 喂给自己的 agent，核对字段 | 每对接口用例通过 |
| D8–9 | 搭调度器：A→E 全链路跑通 + 质检关卡 + 人工确认 + workbench.html | 端到端 ≤ 5 分钟 |
| D10–12 | 演示剧本彩排 3 遍；录屏 + 字幕 | 演示稳定不翻车 |
| D13–14 | 设计文档（AI 结合环节细节）+ 商业分析 + 答辩准备 | 全部材料齐 |

---

## 9. 风险与兜底

| 风险 | 兜底 |
|---|---|
| 某成员模块没达标 | 用 samples/ 期望输出直接当 mock 数据，标"转人工"，不阻塞链路 |
| 契约字段对不上 | 以 all_skill 为准，改实现不改接口 |
| AI 输出不合 schema | 退回重做 1 次 → 仍失败则 hold + 转人工 |
| 演示现场模型不稳定 | 主链路提前跑通并录屏备份；现场用录屏兜底 |
| API 费用 | DeepSeek 很便宜；开发期省着用（批量验证，别一句一调）；按课程方式充值小额即可 |
| 排错卡住 | 把完整报错丢给 AI 修；再不行找老师/看官方 reference 文档 |

---

## 10. 四份文档的关系（贴墙上）

```
DESP技能规范接口v1.0.md   ← 一个技能怎么写（字段规范）
        ↓
all_skill总集成文档.md     ← 5 个技能怎么合成一个系统（接口/目标/合并）
        ↓
团队分工与交付说明.md       ← 谁做什么、交什么、怎么验收
        ↓
DSH开发路线图v1.0.md       ← 本文件：在 DSH 上按顺序落地以上三份
```

**开工顺序**：先签契约（all_skill 2.2）→ 每人写 ROLE → 单人闭环 → 合并 → 打磨。契约冻结之前，任何实现都是浪费。
