# 团队分工与交付说明（DSH 版）

> 适用：5 人学生团队，HR 数字员工方向（多智能体协同），开发平台 = DeepSeek Harness (DSH)。
> 配套文档：《DESP技能规范接口v1.0.md》（单技能契约）、《all_skill总集成文档.md》（合并契约）、《DSH开发路线图v1.0.md》（怎么落地）。
> 核心原则：**每人交付"可验收的工作成果"（报告 / 表格 / 网页 / 文档），而不是"一段对话"**。
> 工作方式：**活由 DSH 里的 AI 干，人负责下指令、拆任务、验收成果**——不需要自己写代码。

---

## 0. 总览

| 成员 | 数字员工 | 一句话职责 | 主要可验收成果 |
|---|---|---|---|
| A | 招聘分析师 | 模糊诉求 → 结构化 JD + 能力画像 | 需求澄清报告、结构化 JD、能力画像表 |
| B | 简历猎手 | 简历解析 + 初筛打分 | 简历结构化表、初筛打分表 |
| C | 面试官 | 出题 + 追问 + 纪要 | 面试题纲、面试纪要报告、面试评分表 |
| D | 测评背调员 | 技能测评 + 真实性核查 + 文化匹配 | 测评报告、风险清单表、文化匹配报告 |
| E | offer 与入职管家 | offer 生成 + 入职计划 | offer 草案(文档/PDF)、谈薪建议表、入职计划(网页) |

**依赖链（决定合并顺序）**：A → B → C → D → E

---

## 1. 统一技术栈（先读这段，再各自开工）

### 1.1 开发平台（全员统一：DSH）
- 每人一台电脑跑 DSH（Web 界面），**所有实现工作由 DSH 会话里的 AI agent 完成**；
- 人要做的事：**下清晰指令 → 检查产出 → 把报错丢回给 AI 修**；
- 大模型走 DeepSeek API（每台电脑按课程指导配好 key）；
- 协作：git 仓库，一人一个模块目录，接口先行（详见《DSH开发路线图v1.0.md》）。

### 1.2 交付层（成果仍按类型产出）
| 成果类型 | 技术方案 |
|---|---|
| 报告 | agent 生成 Markdown → 导出 PDF / Word |
| 表格 | agent 生成 CSV（Excel 可直接打开） |
| 网页 | 生成 HTML（最后统一汇总成"成果工作台"页面） |
| 文档 | Markdown / PDF |

### 1.3 每个人的开发动作（不是写代码，是"指挥 AI 产出定义文件"）
每人维护自己的数字员工模块（一个文件夹），内容全部可以让 AI 帮你生成：
- **角色提示词（ROLE）**：你是谁、职责、输出契约、风格规则；
- **技能样例**：每个技能的输入 JSON + 期望输出 JSON（对齐 all_skill 的数据契约）；
- **自测**：把你的模块喂给任意一个 DSH 会话，看它能否按契约产出成果文件。

---

## 2. 成员 A — 招聘分析师

- **技能**：`demand.clarify`、`jd.generate`、`persona.build`
- **职责**：把业务方一句模糊诉求，澄清成结构化需求，再生成 JD 与候选人画像

### 交付目标（可验收成果）
| # | 成果 | 类型 | 内容 |
|---|---|---|---|
| 1 | 需求澄清报告 | 报告 | 澄清后需求陈述 + 待确认信息 + 追问清单 |
| 2 | 结构化 JD | 文档/网页 | 标题 / 职责 / 硬性要求 / 加分项 |
| 3 | 候选人能力画像 | 表格 | 核心能力 / 软性特质 / 文化适配提示 |

### 技术栈（DSH）
角色提示词 × 3（对应 3 个技能）+ 共享知识（团队/公司背景，放 `shared/knowledge/`）+ 成果写成 md / csv 文件

### 验收标准
- 输入一句模糊诉求（如"招个会做数据的人"），能产出 3 类成果；
- JD 四个字段完整，画像三个字段完整；
- 追问清单 ≥ 3 条。

### 依赖
- 上游：无（全流程起点）
- 下游：B（把 JD 交给简历猎手）

---

## 3. 成员 B — 简历猎手

- **技能**：`resume.parse`、`resume.score`
- **职责**：把任意简历解析成结构化字段，并按 JD 打分

### 交付目标（可验收成果）
| # | 成果 | 类型 | 内容 |
|---|---|---|---|
| 1 | 简历结构化表 | 表格 | 姓名 / 年限 / 技能 / 经历 / 学历 |
| 2 | 初筛打分表 | 表格 | 每份简历的 score / verdict / matched / gaps / reason |

### 技术栈（DSH）
简历文本（OCR/PDF 文本提取按需）+ 解析/打分提示词 + 自测样例 3 份简历 + 输出 csv 表格

### 验收标准
- 输入简历原文，输出 5 个结构化字段完整；
- 每份简历有 0–100 分 + `verdict` 判定 + `reason`；
- 多份简历能并行处理。

### 依赖
- 上游：A（JD）
- 下游：C（打分结果交给面试官）

---

## 4. 成员 C — 面试官

- **技能**：`interview.plan`、`interview.run`
- **职责**：按 JD 和简历出题，动态追问，生成面试纪要

### 交付目标（可验收成果）
| # | 成果 | 类型 | 内容 |
|---|---|---|---|
| 1 | 面试题纲 | 文档 | 问题列表 + 提问意图 + 评分维度权重 |
| 2 | 面试纪要报告 | 报告 | 摘要 + 逐题打分 + 录用判定 |
| 3 | 面试评分表 | 表格 | 维度 × 权重 × 得分 |

### 技术栈（DSH）
出题/纪要提示词 + 样例问答对 + 输出 md / csv（语音转写 ASR 可选加分）

### 验收标准
- 输入 JD + 简历，输出结构化题纲；
- 输入问答记录，输出摘要 + 判定 + 逐题评分；
- 追问逻辑能针对回答动态调整（体现"自主规划"）。

### 依赖
- 上游：B（简历 + 打分）
- 下游：D（纪要交给测评背调员）

---

## 5. 成员 D — 测评背调员

- **技能**：`assess.skill`、`assess.integrity`、`assess.culture`
- **职责**：技能测评、简历/面试真实性核查、团队文化匹配

### 交付目标（可验收成果）
| # | 成果 | 类型 | 内容 |
|---|---|---|---|
| 1 | 技能测评报告 | 报告 | 能力等级 / 分数 / 说明 |
| 2 | 真实性核查风险清单 | 表格 | 发现项 / 风险等级 / 证据 |
| 3 | 文化匹配报告 | 报告 | fit_score / 补位能力 / 冗余能力 |

### 技术栈（DSH）
测评三类提示词 + 团队画像（`shared/knowledge/`）+ 输出 md / csv

### 验收标准
- 输出 `level`（expert/proficient/basic/none）、`risk_level`（low/medium/high）、`fit_score`（0–100）均落在统一枚举内；
- 风险清单每项都带证据。

### 依赖
- 上游：C（面试纪要）
- 下游：E（综合测评结论交给 offer 管家）

---

## 6. 成员 E — offer 与入职管家

- **技能**：`offer.generate`、`onboard.plan`
- **职责**：生成 offer 与谈薪建议，产出入职计划

### 交付目标（可验收成果）
| # | 成果 | 类型 | 内容 |
|---|---|---|---|
| 1 | offer 草案 | 文档/PDF | 职位 / 薪资区间 / 建议 / 条款 |
| 2 | 谈薪建议表 | 表格 | 基于市场数据的区间建议 |
| 3 | 30 天入职计划 | 网页 | 周计划 + 目标 + 反哺筛选建议 |

### 技术栈（DSH）
offer/入职提示词 + 薪资基准（`shared/knowledge/`）+ 成果 md / csv / 独立 HTML

### 验收标准
- offer 四个字段完整，可导出 PDF；
- 入职计划网页可打开，含 4 周计划 + 目标；
- 有"反哺筛选标准"的改进建议（招聘飞轮闭环）。

### 依赖
- 上游：D（测评结论）
- 下游：无（全流程终点）

---

## 7. 可验收成果汇总表（与平台无关，逐项验收）

| 交付物 | 类型 | 负责 | 验收方式 |
|---|---|---|---|
| 需求澄清报告 | 报告 | A | 有澄清陈述 + 追问清单 |
| 结构化 JD | 文档/网页 | A | 4 字段完整 |
| 候选人能力画像 | 表格 | A | 3 字段完整 |
| 简历结构化表 | 表格 | B | 5 字段完整 |
| 初筛打分表 | 表格 | B | score + verdict + reason |
| 面试题纲 | 文档 | C | 问题 + 评分维度 |
| 面试纪要报告 | 报告 | C | 摘要 + 判定 |
| 面试评分表 | 表格 | C | 维度 × 权重 × 得分 |
| 技能测评报告 | 报告 | D | 等级 + 分数 |
| 真实性风险清单 | 表格 | D | 发现项 + 风险 |
| 文化匹配报告 | 报告 | D | fit_score |
| offer 草案 | 文档/PDF | E | 4 字段完整 |
| 入职计划 | 网页 | E | 可打开 + 周计划 |

> 共 13 类成果：报告 ×5、表格 ×6、文档 ×4、网页 ×2（部分成果跨类型）。**全部覆盖赛题点名的"报告、表格、网页"**，且每一项都能被客观验收。

---

## 8. 共享层分工（不占第 6 个人，全员共同）

| 共享组件 | 负责人建议 | 内容 |
|---|---|---|
| 调度器 + 技能注册表 | 全员共同设计，1 人轮值实现 | 读契约、规划、路由、验收 |
| 任务总线 / 共享状态 | 1 人轮值 | `shared/state/` 候选人档案 JSON |
| 共享知识库 | 1 人轮值 | `shared/knowledge/` 团队背景、薪资、术语 |
| 风格白皮书维护 | 1 人轮值 | 术语表、枚举、四段式骨架 |
| 演示脚本 + 设计文档 | 1 人牵头，全员补充 | 录屏、解说、答辩材料 |

**建议轮值规则**：共享层 5 个组件，每人认领 1 个，同时不影响各自负责的数字员工。

---

## 9. 协作节奏（2 周，DSH 版细节见《DSH开发路线图v1.0.md》）

| 阶段 | 全员 | 个人 |
|---|---|---|
| D1–2 | 装环境跑通"Hello"；冻结接口：三份文档签字 | 各自把数据契约抄进自己的模块 |
| D3–6 | — | 让 AI 按 ROLE + 契约实现自己的 2~3 个技能，单人闭环产出示例成果 |
| D7 | 两两对接（A↔B、B↔C…），核对 JSON 字段 | 修契约 |
| D8–9 | 合并：搭调度器跑通 A→E 全链路 | 修数据契约 |
| D10–14 | 打磨演示 + 录视频 + 写材料 | 完善交付物 |

---

*分工说明到此。接口契约、共同目标与合并计划见《all_skill总集成文档.md》。*
